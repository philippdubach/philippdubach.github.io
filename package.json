{
  "name": "philippdubach.com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "hugo server --disableFastRender",
    "build": "hugo --gc --minify --panicOnWarning --cleanDestinationDir",
    "check": "npm run build && node scripts/check-theme.mjs && node scripts/check-build.mjs && node scripts/check-layout.mjs && node scripts/check-migration.mjs . /private/tmp/pdd-main-worktree"
  },
  "engines": {
    "node": ">=22"
  }
}
