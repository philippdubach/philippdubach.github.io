{{- /*
  Markdown variant of the {{< newsletter >}} shortcode. The HTML version
  emits a full subscribe form (input + button + JS); none of that has
  meaning in markdown — nobody subscribes via curl. Emit nothing.
*/ -}}
