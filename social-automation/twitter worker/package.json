{
  "name": "twitter-poster",
  "version": "1.0.0",
  "description": "Cloudflare Worker to auto-post RSS updates to Twitter",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@social/shared": "file:../shared"
  }
}
