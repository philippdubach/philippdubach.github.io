{
  "name": "social-automation",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.19.0 <25"
  },
  "workspaces": [
    "shared",
    "security-headers",
    "goatcounter-worker",
    "build-trigger",
    "bluesky worker",
    "twitter worker"
  ],
  "scripts": {
    "validate-toolchain": "node scripts/validate-toolchain.mjs",
    "test": "npm run validate-toolchain && node --test test/*.test.mjs && npm run test --workspaces --if-present",
    "check": "npm run validate-toolchain && npm run check --workspaces --if-present"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
